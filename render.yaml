services:
  - type: web
    name: volna-live-intelligence
    runtime: python
    plan: free
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend_app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/live/health
    autoDeploy: true
